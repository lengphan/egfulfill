node_modules/
.expo/
dist/
*.log

# Generated by `expo prebuild`. app.json is the source of truth and EAS prebuilds in
# the cloud — a committed native project silently stops config-plugin changes applying.
/android
/ios
