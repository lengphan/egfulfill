import { BoardPreview } from "@/components/app/designer-preview"

export const metadata = { title: "Board · preview · EGFUL" }

export default function BoardPreviewPage() {
  return <BoardPreview />
}
